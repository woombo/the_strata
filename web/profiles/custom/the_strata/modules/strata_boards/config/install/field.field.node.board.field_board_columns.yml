langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_board_columns
    - node.type.board
    - taxonomy.vocabulary.board_column
  module:
    - options
id: node.board.field_board_columns
field_name: field_board_columns
entity_type: node
bundle: board
label: Board Columns
description: 'Select the columns to display on this board. Leave empty to show all columns.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:taxonomy_term'
  handler_settings:
    target_bundles:
      board_column: board_column
    sort:
      field: weight
      direction: asc
    auto_create: false
field_type: entity_reference
