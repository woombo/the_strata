langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_ticket_type
    - node.type.ticket
    - taxonomy.vocabulary.ticket_type
id: node.ticket.field_ticket_type
field_name: field_ticket_type
entity_type: node
bundle: ticket
label: Type
description: 'The type of ticket (Bug, Feature, Task, etc.).'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:taxonomy_term'
  handler_settings:
    target_bundles:
      ticket_type: ticket_type
    sort:
      field: name
      direction: asc
    auto_create: true
field_type: entity_reference
